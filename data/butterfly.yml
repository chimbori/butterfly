# butterfly.yml

web:
  host: "localhost" # Set `host` to `localhost` (instead of ``) to prevent the macOS firewall dialog.
  port: 9999
  public-url: 'https://bfly.chimbori.net/'

link-preview:
  domains:
  - chimbori.com
  - manas.tungare.name
  # screenshot:
  #   timeout: 20s
  # cache:
  #   enabled: false

# debug: true
